<div class="container col-6">

  <div class="justify-content-center m-3">
   <h2>PAST SEARCH</h2>
    <div>
        <% @contexts.each do |context| %>
          <div class="border m-2">
            <h4 class="m-2"><%= context.objective %></h4>
            <%= link_to "Go for this context", context_path(context) %>
          </div>
        <% end %>
    </div>
  </div>




<div class="bg-gradient pt-3">
  <div class="border-0 shadow-lg w-100">
    <div class="p-5">
      <%= simple_form_for [@user, @context] do |f| %>
        <%= f.input :objective,
            placeholder: "What type of outfit are you looking for?",
            required: true,
            label: false,
            input_html: { class: "form-control form-control-lg border-0 shadow-sm mb-4" } %>

        <%= f.input :budget,
            collection: %w[€ €€ €€€ €€€€],
            input_html: { class: "form-select form-select-lg border-0 shadow-sm mb-4" } %>

        <%= f.submit "Find my look!",
            class: "btn btn-dark btn-lg w-100 shadow" %>
      <% end %>
    </div>
  </div>
</div>
